spring:
  jpa:
    hibernate:
      ddl-auto: none
  
  datasource:
    url: jdbc:mysql://localhost:3306/sample
    username: root
    password: mysql

flyway:
  url: jdbc:mysql://localhost:3306/sample
  user: root
  password: mysql